import type { CardEffect } from '../../unstable-unicorns/engine/effects/CardEffect.ts';

export const specialDelivery: CardEffect = {
  // The choice is resolved by the nursery-selection handler.
  onPlay() {},
};
