import type { CardEffect } from '../../unstable-unicorns/engine/effects/CardEffect.ts';

export const rainbowSprinkles: CardEffect = {
  // The choice is resolved by the choice handler.
  onPlay() {},
};
