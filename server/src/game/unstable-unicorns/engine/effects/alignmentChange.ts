import type { GameState } from '../../../models/GameState.ts';
import type { Player } from '../../../models/Player.ts';
import type { Card } from '../../../models/Card.ts';

export function alignmentChange(state: GameState, player: Player, card: Card) {
  // Efecto aqui
}
